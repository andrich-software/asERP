<Page x:Class="asToolkit.Client.Features.Invoices.Views.InvoiceListPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:asToolkit.Client.Features.Invoices.Views"
      xmlns:models="using:asToolkit.Client.Features.Invoices.Models"
      xmlns:dtos="using:asToolkit.Domain.Dtos.Invoice"
      xmlns:mvux="using:Uno.Extensions.Reactive.UI"
      xmlns:utu="using:Uno.Toolkit.UI"
      xmlns:uen="using:Uno.Extensions.Navigation.UI"
      xmlns:controls="using:asToolkit.Client.Controls"
      Background="{ThemeResource BackgroundBrush}">

  <Grid utu:SafeArea.Insets="VisibleBounds">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <!-- Cloudflare-style page header: big title + description, actions top-right -->
    <controls:PageHeader Grid.Row="0"
                         x:Uid="InvoiceListPage.PageHeader"
                         Glyph="&#xE9F9;"
                         Margin="16,0">
      <controls:PageHeader.Actions>
        <StackPanel Orientation="Horizontal" Spacing="8">
          <Button Style="{StaticResource OutlinedButtonStyle}"
                  Command="{Binding Refresh, ElementName=InvoicesFeedView}">
            <FontIcon Glyph="&#xE72C;" FontSize="16" />
          </Button>
          <Button x:Uid="InvoiceListPage.HeaderButton.New"
                  Style="{StaticResource FilledButtonStyle}"
                  Click="CreateInvoice_Click" />
        </StackPanel>
      </controls:PageHeader.Actions>
    </controls:PageHeader>

    <!-- Search Box -->
    <Grid Grid.Row="1"
          Margin="16,12,16,8">
      <TextBox x:Name="SearchBox"
               x:Uid="InvoiceListPage.SearchBox"
               TextChanged="SearchBox_TextChanged"
               Padding="40,0,12,0" />
      <FontIcon Glyph="&#xE721;"
                FontSize="16"
                Foreground="{ThemeResource OnSurfaceVariantBrush}"
                HorizontalAlignment="Left"
                VerticalAlignment="Center"
                Margin="12,0,0,0"
                IsHitTestVisible="False" />
    </Grid>

    <!-- Invoice Table: sortable header (page DataContext) + virtualized ListView body -->
    <Border Grid.Row="2"
            Margin="16,0,16,16"
            Style="{StaticResource TableCardStyle}">
      <Grid>
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto" />
          <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Table Header -->
        <Border Grid.Row="0"
                Background="{ThemeResource SurfaceBrush}"
                CornerRadius="8,8,0,0"
                BorderBrush="{ThemeResource OutlineVariantBrush}"
                BorderThickness="0,0,0,1">
          <Grid>
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="120" />
              <ColumnDefinition Width="*" />
              <ColumnDefinition Width="120" />
              <ColumnDefinition Width="120" />
              <ColumnDefinition Width="150" />
            </Grid.ColumnDefinitions>

            <controls:SortHeaderButton Grid.Column="0"
                                       x:Uid="InvoiceListPage.Header.InvoiceNumber"
                                       SortField="InvoiceNumber"
                                       ActiveSortField="{Binding ActiveSortField}"
                                       SortAscending="{Binding SortAscending}"
                                       Click="SortHeader_Click" />
            <controls:SortHeaderButton Grid.Column="1"
                                       x:Uid="InvoiceListPage.Header.CustomerName"
                                       SortField="CustomerName"
                                       ActiveSortField="{Binding ActiveSortField}"
                                       SortAscending="{Binding SortAscending}"
                                       Click="SortHeader_Click" />
            <controls:SortHeaderButton Grid.Column="2"
                                       x:Uid="InvoiceListPage.Header.Total"
                                       SortField="Total"
                                       ActiveSortField="{Binding ActiveSortField}"
                                       SortAscending="{Binding SortAscending}"
                                       Click="SortHeader_Click" />
            <controls:SortHeaderButton Grid.Column="3"
                                       x:Uid="InvoiceListPage.Header.Status"
                                       SortField="InvoiceStatus"
                                       ActiveSortField="{Binding ActiveSortField}"
                                       SortAscending="{Binding SortAscending}"
                                       Click="SortHeader_Click" />
            <controls:SortHeaderButton Grid.Column="4"
                                       x:Uid="InvoiceListPage.Header.InvoiceDate"
                                       SortField="InvoiceDate"
                                       ActiveSortField="{Binding ActiveSortField}"
                                       SortAscending="{Binding SortAscending}"
                                       Click="SortHeader_Click" />
          </Grid>
        </Border>

        <!-- Table Body -->
        <mvux:FeedView x:Name="InvoicesFeedView"
                       Grid.Row="1"
                       Source="{Binding Invoices}">
          <mvux:FeedView.ValueTemplate>
            <DataTemplate>
              <ListView ItemsSource="{Binding Data}"
                        Style="{StaticResource TableListViewStyle}"
                        ItemContainerStyle="{StaticResource TableItemContainerStyle}"
                        ItemClick="InvoiceRow_Click">
                <ListView.ItemTemplate>
                  <DataTemplate x:DataType="dtos:InvoiceListDto">
                    <Grid BorderBrush="{ThemeResource OutlineVariantBrush}"
                          BorderThickness="0,0,0,1">
                      <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="120" />
                        <ColumnDefinition Width="120" />
                        <ColumnDefinition Width="150" />
                      </Grid.ColumnDefinitions>

                      <!-- InvoiceNumber -->
                      <TextBlock Grid.Column="0"
                                 Text="{x:Bind InvoiceNumber}"
                                 Style="{StaticResource BodyMedium}"
                                 Foreground="{ThemeResource OnSurfaceBrush}"
                                 VerticalAlignment="Center"
                                 Padding="16,14" />

                      <!-- CustomerName -->
                      <TextBlock Grid.Column="1"
                                 Text="{x:Bind CustomerName}"
                                 Style="{StaticResource BodyMedium}"
                                 Foreground="{ThemeResource OnSurfaceBrush}"
                                 VerticalAlignment="Center"
                                 Padding="16,14"
                                 TextTrimming="CharacterEllipsis" />

                      <!-- Total -->
                      <TextBlock Grid.Column="2"
                                 Text="{x:Bind Total, Converter={StaticResource CurrencyConverter}}"
                                 Style="{StaticResource BodyMedium}"
                                 Foreground="{ThemeResource OnSurfaceBrush}"
                                 VerticalAlignment="Center"
                                 Padding="16,14" />

                      <!-- Status -->
                      <controls:StatusBadge Grid.Column="3"
                                            Status="{x:Bind InvoiceStatus}"
                                            Padding="10,4"
                                            VerticalAlignment="Center"
                                            Margin="16,8" />

                      <!-- InvoiceDate -->
                      <TextBlock Grid.Column="4"
                                 Text="{x:Bind InvoiceDate, Converter={StaticResource DateOnlyConverter}}"
                                 Style="{StaticResource BodyMedium}"
                                 Foreground="{ThemeResource OnSurfaceVariantBrush}"
                                 VerticalAlignment="Center"
                                 Padding="16,14" />
                    </Grid>
                  </DataTemplate>
                </ListView.ItemTemplate>
              </ListView>
            </DataTemplate>
          </mvux:FeedView.ValueTemplate>

          <mvux:FeedView.ProgressTemplate>
            <DataTemplate>
              <ProgressRing IsActive="True"
                            Width="48"
                            Height="48"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Margin="48" />
            </DataTemplate>
          </mvux:FeedView.ProgressTemplate>

          <mvux:FeedView.NoneTemplate>
            <DataTemplate>
              <controls:EmptyState x:Uid="InvoiceListPage.NoInvoices"
                                   Glyph="&#xE9F9;"
                                   Background="Transparent"
                                   VerticalAlignment="Center" />
            </DataTemplate>
          </mvux:FeedView.NoneTemplate>

          <mvux:FeedView.ErrorTemplate>
            <DataTemplate>
              <controls:EmptyState x:Uid="InvoiceListPage.LoadError"
                                   Glyph="&#xE783;"
                                   Background="Transparent"
                                   VerticalAlignment="Center" />
            </DataTemplate>
          </mvux:FeedView.ErrorTemplate>
        </mvux:FeedView>
      </Grid>
    </Border>

    <!-- Pagination Controls -->
    <Border Grid.Row="3"
            Background="{ThemeResource SurfaceBrush}"
            BorderBrush="{ThemeResource OutlineVariantBrush}"
            BorderThickness="0,1,0,0"
            Padding="16,8">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Count Info (Left) -->
        <mvux:FeedView Source="{Binding Pagination}"
                       Grid.Column="0">
          <mvux:FeedView.ValueTemplate>
            <DataTemplate>
              <TextBlock Text="{Binding Data.CountInfo}"
                         Style="{StaticResource BodySmall}"
                         Foreground="{ThemeResource OnSurfaceVariantBrush}"
                         VerticalAlignment="Center" />
            </DataTemplate>
          </mvux:FeedView.ValueTemplate>
        </mvux:FeedView>

        <!-- Page Navigation (Center) -->
        <StackPanel Grid.Column="1"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    Spacing="8">
          <Button x:Name="PreviousPageButton"
                  Style="{StaticResource IconButtonStyle}"
                  x:Uid="InvoiceListPage.PreviousPage"
                  Click="PreviousPage_Click">
            <FontIcon Glyph="&#xE76B;"
                      FontSize="16" />
          </Button>

          <mvux:FeedView Source="{Binding Pagination}">
            <mvux:FeedView.ValueTemplate>
              <DataTemplate>
                <TextBlock Text="{Binding Data.PageInfo}"
                           Style="{StaticResource BodyMedium}"
                           VerticalAlignment="Center"
                           MinWidth="100"
                           TextAlignment="Center" />
              </DataTemplate>
            </mvux:FeedView.ValueTemplate>
          </mvux:FeedView>

          <Button x:Name="NextPageButton"
                  Style="{StaticResource IconButtonStyle}"
                  x:Uid="InvoiceListPage.NextPage"
                  Click="NextPage_Click">
            <FontIcon Glyph="&#xE76C;"
                      FontSize="16" />
          </Button>
        </StackPanel>

        <!-- Page Size Selection (Right) -->
        <StackPanel Grid.Column="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Spacing="8">
          <TextBlock x:Uid="InvoiceListPage.ItemsPerPage"
                     Style="{StaticResource BodySmall}"
                     Foreground="{ThemeResource OnSurfaceVariantBrush}"
                     VerticalAlignment="Center" />
          <ComboBox x:Name="PageSizeComboBox"
                    Style="{StaticResource PageSizeComboBoxStyle}"
                    SelectionChanged="PageSizeComboBox_SelectionChanged"
                    SelectedIndex="1">
            <ComboBoxItem Content="10" Tag="10" />
            <ComboBoxItem Content="25" Tag="25" />
            <ComboBoxItem Content="100" Tag="100" />
            <ComboBoxItem Content="500" Tag="500" />
            <ComboBoxItem Content="1000" Tag="1000" />
          </ComboBox>
        </StackPanel>
      </Grid>
    </Border>
  </Grid>
</Page>
